/* import { createSlice } from "@reduxjs/toolkit";

const userSlice = createSlice({
  name: "user",
  initialState: { value: {id: "", username: "", admin: false} },
  reducers: {
      login: (state, action) => {

      }
  }
}); */
